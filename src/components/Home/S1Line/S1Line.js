import React from 'react';
import style from '../Home.module.css'

const S1Line = (props) => {
    // if (props.screenMode === "mobile") {
    //     return (
    //         <div className={style.lines}>
    //             <h1 className={style[props.weight]}>student.</h1>
    //         </div>
    //     )
    // } else if (props.screenMode === "desktop") {
        return (
            <div className={style.lines}>
                <h1>HEllo</h1>
            </div>
        )
    // }
    
}

export default S1Line;